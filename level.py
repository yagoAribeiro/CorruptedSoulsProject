class Level:

    def __init__(self, name, number, boss_id, thread_level):
        name = name
        number = number
        boss_id = boss_id
        thread_level = thread_level
        background_img = []

